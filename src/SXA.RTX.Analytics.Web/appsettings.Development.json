{
  "DetailedErrors": true,
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft": "Information",
        "Microsoft.AspNetCore": "Information"
      }
    }
  },
  // Local development: set your real connection string via `dotnet user-secrets`:
  //   dotnet user-secrets set "ConnectionStrings:ConfigurationDatabase" "Server=...;Database=SXA_RTX_Analytics;..."
  // Leaving empty uses in-memory provider so the app boots without SQL Server.
  "ConnectionStrings": {
    "ConfigurationDatabase": ""
  }
}
